import COLORS from '@utils/colors'

const boxShadow = {
  shadowColor: COLORS.gray,
  shadowOffset: {
    width: 10,
    height: 20
  },
  shadowOpacity: 0.05,
  shadowRadius: 3.84,
  elevation: 10
}

export default boxShadow
